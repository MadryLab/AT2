[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "at2"
version = "0.0.1"
authors = [
  { name="Benjamin Cohen-Wang", email="bencw@mit.edu" },
]
description = "Attribute statements generated by LLMs to preceding tokens using attention weights."
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = ["numpy", "torch", "transformers", "datasets", "pandas", "nltk >= 3.8.2", "spacy", "scikit-learn", "tqdm"]

[project.optional-dependencies]
optional = ["beautifulsoup4"]
test = ["pytest"]


[tool.setuptools.packages.find]
exclude = ["notebooks*", "assets*", "tests*"]

[project.urls]
Homepage = "https://github.com/MadryLab/AT2"
Issues = "https://github.com/MadryLab/AT2/issues"